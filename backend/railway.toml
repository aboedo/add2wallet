[build]
builder = "dockerfile"
buildCommand = "docker build -t add2wallet-backend ."
watchPatterns = ["**/*.py", "requirements.txt", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300

[environments.production]
variables = { REVENUECAT_SECRET_KEY = "sk_xYDUixBpiCkUQiwlmeMlCvvFrGjNv" }